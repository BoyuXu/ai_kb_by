# 生成式推荐系统技术演进综合总结
> 学习日期：20260326 | 论文数：10篇

## 📋 核心主题

2026年推荐系统领域的主旋律是**生成式统一**：从单一功能模型向统一生成框架演进，LLM 从外挂变核心，多任务从串行变并行。

---

## 📚 参考文献

1. HSTU: Trillion-Parameter Sequential Transducers for Generative Recommendations (Meta, 2024)
2. GPR: Towards a Generative Pre-trained One-Model Paradigm for Large-Scale Advertising Recommendation
3. OneRanker: Unified Generation and Ranking with One Model in Industrial Advertising Recommendation
4. OneTrans: Unified Feature Interaction and Sequence Modeling with One Transformer
5. RecGPT: Technical Report on Next-Generation Recommendation System with LLM
6. E2E Semantic ID Generation for Generative Recommendation Systems
7. OneSug: The Unified End-to-End Generative Framework for E-commerce Query Suggestion
8. Align³GR: Unified Multi-Level Alignment for LLM-based Generative Recommendation
9. MTMH: Multi-Task Multi-Head Approach for Item-to-Item Retrieval
10. Multi-Task Learning with Task-Aware Routing for Recommendation

---

## 📐 核心公式

### 1. HSTU 线性注意力（万亿参数序列推荐）
$$\text{LinearAttn}(Q, K, V) = \phi(Q) \cdot (\phi(K)^T V)$$

其中 $\phi$ 为核函数近似，将 $O(n^2d)$ 降为 $O(nd^2)$，使超长用户序列建模成为可能。

### 2. RQ-VAE Semantic ID 生成（分层量化）
$$z_q^{(k)} = \arg\min_{c \in \mathcal{C}_k} \|z - \sum_{j<k} z_q^{(j)} - c\|_2$$

端到端训练时用 Straight-Through Estimator 处理不可微的 argmin：
$$\frac{\partial \mathcal{L}}{\partial z} \approx \frac{\partial \mathcal{L}}{\partial z_q}$$

### 3. Triple Alignment（Align³GR）
$$\mathcal{L}_\text{total} = \mathcal{L}_\text{semantic} + \lambda_1 \mathcal{L}_\text{task} + \lambda_2 \mathcal{L}_\text{preference}$$

$$\mathcal{L}_\text{dpo} = -\log \sigma\left(\beta \log \frac{\pi_\theta(y_w|x)}{\pi_\text{ref}(y_w|x)} - \beta \log \frac{\pi_\theta(y_l|x)}{\pi_\text{ref}(y_l|x)}\right)$$

### 4. MoE Task-Aware Routing（多任务路由）
$$h^{(t)} = \sum_{k=1}^K g_k^{(t)}(x) \cdot E_k(x), \quad g_k^{(t)}(x) = \text{Softmax}(W_t [x; e_t])_k$$

### 5. InfoNCE 对比损失（I2I 多头检索）
$$\mathcal{L}_{k} = -\log \frac{\exp(\text{sim}(q_k, p_k)/\tau)}{\sum_j \exp(\text{sim}(q_k, n_{kj})/\tau)}$$

---

## 🎓 Q&A 面试精华

**Q1: 为什么生成式推荐成为行业主流趋势？**
A: 三大驱动力：①统一多阶段（召回→排序统一为生成）消除 Stage Gap ②LLM 预训练的语义理解能力迁移（冷启动+语义匹配）③Token 化 item 表示（Semantic ID）使推荐=语言生成，享受 LLM 基础设施红利。

**Q2: HSTU 和 SASRec 处理长序列的核心差异？**
A: SASRec：标准 Transformer，O(n²) attention，序列长度受限（通常<200）。HSTU：线性注意力近似，O(n)，支持数万步历史；分层结构（局部+全局）；参数量可扩展至万亿（embedding table 主导）。

**Q3: Semantic ID vs 协同过滤 ID，如何在实际系统中融合？**
A: 双路并行：CF embedding 捕获协同信号（买了A又买B），Semantic ID 捕获语义信号（标题描述相似）；融合：concat 后接 MLP，或用注意力机制自适应加权；冷启动时降权 CF，提权 Semantic ID。

**Q4: 多任务推荐中梯度冲突的根本原因和解决方案？**
A: 根因：CTR 优化方向（最大化点击）与 CVR 优化方向（最大化转化）在参数空间中不总是一致（梯度余弦夹角<0）。解决：①PCGrad/GradNorm：梯度投影/归一化 ②MoE：物理隔离任务参数 ③Task-Aware Routing：动态路由不同样本到不同专家。

**Q5: OneRanker 联合训练召回和精排的工程挑战？**
A: 主要挑战：①样本空间不一致（召回用全库，精排用曝光子集）②推理阶段分离（生成头用 ANN，排序头用 MLP）③梯度平衡（召回 Loss 和排序 Loss 量级不同）。工程方案：独立数据加载器 + 梯度归一化 + 分阶段推理接口。

**Q6: 生成式召回（GPR/OneRanker）的延迟问题如何在工业系统中解决？**
A: 分层 Beam Search（先粗后细）+ Speculative Decoding（草稿模型预测）+ Prefix Tree 约束解码 + 候选缓存（高频查询预计算）。最终目标：召回延迟 <50ms，对比向量 ANN 检索 5ms，差距约 10x，需要合理划定适用场景。

**Q7: RecGPT 等 LLM 推荐系统的幻觉风险如何控制？**
A: 三层防护：①约束解码（只生成库内 item 的 token）②RAG 验证（生成前检索商品库确认存在）③后处理过滤（不在库中的 item 直接丢弃）。根本上：不让 LLM"凭空生成"，而是从候选集中"选择"。

**Q8: E2E Semantic ID 相比两阶段 RQ-VAE 训练的核心优势是什么？**
A: 优化目标对齐：两阶段中 ID 生成目标（重建）与下游推荐目标（排序）不一致，导致 ID 质量在推荐任务上非最优。E2E 用推荐 Loss 直接反传到 ID 生成，使 ID 语义空间直接服务于推荐排序，实验证明可提升 6%+ HR@10。

**Q9: Item-to-Item 检索（MTMH）与 User-to-Item 检索的主要区别？**
A: I2I：query=item，无需个性化，正样本=共现 item，适合离线构建 item 相似图；U2I：query=user，高度个性化，正样本=用户交互 item，实时计算。工业系统通常同时使用：U2I 做个性化召回，I2I 做相关推荐（"看了又看"）。

**Q10: 推荐系统中 ESMM（全空间多任务）框架解决什么核心问题？**
A: 样本选择偏差（SSB）：传统 CVR 模型只在点击样本上训练，但线上需要在全曝光空间预测 CVR，导致训练集和预测集分布不一致（有偏估计）。ESMM 通过 P(CVR)×P(CTR)=P(CTCVR) 的乘积约束，在全空间联合训练，消除 SSB。

---

## 🔑 技术趋势总结

| 趋势 | 代表论文 | 核心思路 |
|------|---------|---------|
| 万亿参数序列模型 | HSTU | 线性注意力 + embedding table 规模化 |
| 统一生成框架 | GPR, OneRanker | 召回+精排同一模型，任务前缀区分 |
| Semantic ID | E2E Semantic ID | RQ-VAE + 端到端推荐优化 |
| LLM 对齐 | Align³GR, RecGPT | 语义/任务/偏好三级对齐 |
| 多任务路由 | MTMH, Task-Aware | MoE + 场景感知动态路由 |
