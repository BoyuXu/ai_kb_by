# MelonEgg 每日学习日报 - 20260323

## 今日处理概览

| 领域 | 篇数 | 核心主题 |
|------|------|----------|
| rec-sys（推荐系统）| 13篇 | 生成式推荐工业落地：OneRec/ETEGRec/Actions Speak Louder |
| ads（广告系统） | 10篇 | LLM融合广告：LEARN/PLUM/Wukong Scaling Law |
| search（搜索）| 16篇 | 稠密/稀疏/生成式检索统一：DPR/ColBERT/SPLADE/BM25S |
| llm-infra（LLM基础设施）| 17篇 | 推理优化/PEFT/Scaling：vLLM/LoRA/DeepSeek-V3 |
| **合计** | **56篇** | **生成式AI全栈** |

---

## 推荐系统（13篇）核心收获

### 今日最重要论文
**OneRec（快手）**：目前工业落地最完整的生成式推荐系统，统一召回+排序，引入迭代偏好对齐，线上用户留存率+1.8%。技术亮点：RQ-VAE物品tokenization + prefix cache加速 + DPO偏好对齐。

### 关键技术点
- 生成式推荐统一框架（OneRec/GPR）：消除多阶段信息损失，联合优化召回排序
- 端到端tokenization（ETEGRec）：tokenizer和推荐模型联合优化，比分离训练+3% NDCG
- 推荐Scaling Law（Actions Speak Louder/Wukong）：Embedding Table scaling优先于MLP
- 推理增强推荐（REG4Rec/Act-With-Think）：32-64 token轻量推理性价比最高

### 面试关键词
`生成式推荐` `RQ-VAE tokenization` `beam search约束解码` `偏好对齐DPO` `Pareto多目标优化`

---

## 广告系统（10篇）核心收获

### 今日最重要论文
**Wukong（Meta）**：推荐/广告系统的Scaling Law研究，发现Embedding Table scaling比MLP更有价值，为资源分配提供理论指导。

### 关键技术点
- LLM知识蒸馏到广告（LEARN/PLUM）：离线蒸馏保证latency，冷启动提升最显著
- GNOLR有序多任务：利用行为序关系约束（曝光→点击→转化）提升CVR预估
- 多模态广告（Qarm）：视频量化对齐，CTR+1.3%，推理速度提升3x
- Scaling Law：万亿参数Embedding配合分布式PS架构

### 面试关键词
`ESMM样本选择偏差` `MMoE多任务学习` `LoRA适配` `Embedding Scaling` `多模态融合`

---

## 搜索（16篇）核心收获

### 今日最重要论文
**BM25S**：纯Python实现，比Elasticsearch快10x，同等精度，是RAG和搜索应用的理想选择。
**ColBERT Late Interaction**：bi-encoder（快）和cross-encoder（精）的中间路线，MaxSim操作。

### 关键技术点
- 检索三角权衡：Bi-encoder（快）← ColBERT（中）→ Cross-encoder（精）
- 稀疏vs稠密：电商精确匹配稀疏好，语义理解稠密好，混合方案最优
- LLM重排（RankLLM）：Sliding window listwise，GPT-4重排NDCG超传统方法8%
- 生成式搜索统一（OneSearch）：统一搜索+推荐框架，冷启动场景+8%

### 面试关键词
`BM25公式` `DPR对比学习` `SPLADE词汇扩展` `ColBERT MaxSim` `RAG评估RAGAS`

---

## LLM基础设施（17篇）核心收获

### 今日最重要论文
**vLLM（PagedAttention）**：工业LLM serving的标准选择，吞吐量提升24x，内存碎片从70%降至4%。
**DeepSeek-V3**：671B MoE模型以~600万美元训练成本达GPT-4级别，MLA+FP8训练的工程典范。

### 关键技术点
- **推理加速三件套**：PagedAttention（内存管理）+ Continuous Batching（利用率）+ Speculative Decoding（速度）
- **PEFT标准方案**：LoRA（参数高效）→ LoRA+（不对称学习率）→ QLoRA（量化节省显存）
- **KV Cache压缩**：INT8量化几乎无损（推荐生产默认）；H2O驱逐保留20% token适合短任务
- **推理能力**：DeepSeek-R1 GRPO + RLVR证明可验证奖励的RL比RLHF更稳定
- **MoE**：DeepSeek-V3的256专家+Fine-grained+MLA是当前最优MoE设计

### 面试关键词
`PagedAttention` `Continuous Batching` `LoRA low-rank` `GRPO可验证奖励` `MoE路由负载均衡` `Speculative Decoding`

---

## 今日跨领域关联

```
生成式AI应用层：推荐/广告/搜索 ← 统一框架趋势（OneRec/OneSearch/统一生成）
        ↕ 共享技术栈
LLM能力层：推理（DeepSeek-R1）/ 对齐（RLVR）/ 知识（LLaMA3/Qwen2.5）
        ↕ 基础设施
LLM推理层：vLLM / SGLang / Speculative Decoding / KV压缩
```

**核心观察**：搜广推三个领域正在向统一生成式框架收敛，底层都依赖LLM基础设施的高效serving。掌握vLLM/LoRA/生成式推荐是2025年算法工程师的必备技能栈。

---

## 面试备考重点（本日新增）

### 高频考题（今日新增8道）

1. **生成式推荐vs传统两阶段**：生成式统一模型消除信息损失，但latency高10-100x
2. **RQ-VAE物品tokenization**：残差量化，物品embedding→层级离散码本，支持beam search
3. **LoRA原理与参数量计算**：ΔW=BA，参数从d²降到2dr，推理零额外开销（权重合并）
4. **PagedAttention原理**：类OS页表，KV cache分块管理，消除内存碎片（70%→4%）
5. **ESMM解决CVR偏差**：在全曝光空间建模CTCVR=CTR×CVR，间接获得无偏CVR
6. **ColBERT Late Interaction**：独立编码（效率）+token级MaxSim（精度），三角权衡中间点
7. **GRPO原理**：组内相对奖励代替绝对值，无需critic网络，训练成本-40%
8. **MoE负载均衡**：auxiliary loss（帮助but影响质量）vs bias调整（DeepSeek无损方案）

---

## 文件统计

```
~/Documents/ai-kb/
├── rec-sys/    13个文件（20260323_*.md）
├── ads/        10个文件（20260323_*.md）
├── search/     16个文件（20260323_*.md）
├── llm-infra/  17个文件（20260323_*.md）
├── synthesis/  2个综合文件（rec-sys + ads）
└── weekly/     20260323.md（本文件）
```

总计：56篇论文笔记 + 2篇综合总结

---

*Generated by MelonEgg | 2026-03-23*
