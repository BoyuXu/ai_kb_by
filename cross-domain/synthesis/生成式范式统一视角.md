# 生成式范式在推荐/广告/搜索中的统一视角

> 📚 参考文献
> - [Action Is All You Need Dual-Flow Generative Ran...](../../ads/papers/Action_is_All_You_Need_Dual_Flow_Generative_Ranking_Netwo.md) — Action is All You Need: Dual-Flow Generative Ranking Netw...
> - [Diffgrm Diffusion Generative Rec](../../rec-sys/papers/DiffGRM_Diffusion_based_Generative_Recommendation_Model.md) — DiffGRM: Diffusion-based Generative Recommendation Model
> - [Bm25-Semantic-Hybrid-Retrieval](../../search/papers/BM25_Hybrid_Retrieval.md) — BM25 与语义检索融合：Hybrid Retrieval 最佳实践
> - [Tbgrecall A Generative Retrieval Model For E-Co...](../../ads/papers/TBGRecall_A_Generative_Retrieval_Model_for_E_commerce_Rec.md) — TBGRecall: A Generative Retrieval Model for E-commerce Re...
> - [Linear-Item-Item-Session-Rec](../../rec-sys/papers/Linear_Item_Item_Model_with_Neural_Knowledge_for_Session.md) — Linear Item-Item Model with Neural Knowledge for Session-...
> - [Deploying-Semantic-Id-Based-Generative-Retrieva...](../../rec-sys/papers/Deploying_Semantic_ID_based_Generative_Retrieval_for_Larg.md) — Deploying Semantic ID-based Generative Retrieval for Larg...
> - [Dense Retrieval Vs Sparse Retrieval A Unified Eval](../../search/papers/Dense_Retrieval_vs_Sparse_Retrieval_A_Unified_Evaluation.md) — Dense Retrieval vs Sparse Retrieval: A Unified Evaluation...
> - 【20260327新增】[GPR Generative Pretraining Ads Ranking](../../ads/papers/GPR_generative_pretraining_ads_ranking.md) — GPR: 生成式预训练统一广告多子任务（CTR/CVR/冷启动）
> - 【20260327新增】[GPR Generative Pre-Trained Recommendation](../../rec-sys/papers/GPR_generative_pre_trained_recommendation.md) — 微信GPR: 生成式预训练统一召回+精排+出价（CTR+7.2%, GMV+5.1%）
> - 【20260327新增】[OneRanker Unified Generation Ranking](../../rec-sys/papers/OneRanker_unified_generation_ranking.md) — OneRanker: 生成分支+排序分支统一架构，消除Stage Gap
> - 【20260327新增】[HSTU Trillion Parameter Sequential Transducers](../../rec-sys/papers/HSTU_trillion_parameter_sequential_transducers.md) — HSTU: Meta万亿参数推荐系统，线性注意力O(n)序列建模
> - 【20260327新增】[RecGPT Next Generation Recommendation LLM](../../rec-sys/papers/RecGPT_next_generation_recommendation_LLM.md) — RecGPT: LLM统一推荐范式

> 创建：2026-03-24 | 更新：2026-03-27 | 领域：跨域 | 类型：综合分析
> 来源：TIGER, ActionPiece, DiffGRM, Generative Retrieval, TBGRecall, GPR(ads+rec), HSTU, OneRanker

## 📐 核心公式与原理

### 1. 自回归生成推荐核心公式

$$
P(r_1, ..., r_k | u, ctx) = \prod_{i=1}^{k} P(r_i | r_1, ..., r_{i-1}, u, ctx)
$$

- 推荐/广告的生成式范式统一形式（GPR-rec, GPR-ads, OneRanker均使用）

### 2. 生成+排序统一损失（OneRanker）

$$
\mathcal{L} = \lambda_1 \mathcal{L}_{gen} + \lambda_2 \mathcal{L}_{rank} + \lambda_3 \mathcal{L}_{align}
$$

- 生成分支 + 排序分支 + 表示对齐损失，一个模型两个输出头

### 3. HSTU线性注意力（O(n)复杂度）

$$
\text{Attention}(Q, K, V) = \phi(Q) \cdot (\phi(K)^T V)
$$

- 核函数替代softmax，先算K^TV（d×d），避免n×n矩阵，支持数万token序列

### 4. Semantic ID量化（RQ-VAE）

$$
c_l = \arg\min_{k} ||z_l - e_k||_2, \quad l = 1,...,L
$$

- 物品被量化为L层级码字，同语义物品共享上层码字

### 5. GPR多阶段统一出价（微信GPR）

$$
P(a_1, ..., a_k | u, c) = \prod_{i=1}^{k} P(a_i | u, c, a_1, ..., a_{i-1})
$$

- 同一个生成式模型完成：召回（生成候选ID）+ 精排（重排序）+ 出价（回归金额）

### 6. 偏差校正 (IPW)

$$
\hat{R} = \frac{1}{n}\sum_i \frac{r_i}{P(O=1|x_i)}
$$

- 逆倾向加权消除选择偏差

---

## 🎯 核心洞察（更新至2026-03-27）

1. **三个领域同时经历"判别→生成"范式转换**：推荐从"打分排序"到"生成下一个物品"，搜索从"匹配检索"到"生成答案"，广告从"竞价排序"到"生成广告多阶段统一"
2. **GPR范式在推荐和广告中同步爆发**：2026-03-27同一天，rec-sys（微信GPR）和ads（GPR for Ads Ranking）都用"生成式预训练"统一多个子任务，证明这是横跨两个领域的通用范式
3. **Semantic ID 是连接三个领域的桥梁**：物品/文档/广告都可以编码为 Semantic Token 序列，统一的自回归框架就能处理推荐、检索、广告匹配
4. **Stage Gap是核心痛点**：GPR(ads)、GPR(rec)、OneRanker都在解决同一个问题——多阶段系统中召回/精排/出价目标不一致导致的整体效果次优
5. **扩展律（Scaling Law）已被推荐系统验证**：HSTU（Meta）在万亿参数推荐系统上验证了scaling law，推荐系统像LLM一样，参数越大效果越好
6. **落地挑战集中在延迟和可控性**：生成式方法推理延迟 10-100x 于传统方法，且输出不完全可控（可能生成不存在的 ID）
7. **LLM 重塑了生成式推荐/搜索的可能性**：大语言模型天然具备生成能力，微调后可以直接做推荐（prompt："根据用户历史推荐"）和搜索（生成回答+引用）

---

## 📈 技术演进脉络（更新版）

```
推荐：CF/打分排序 → 双塔向量召回 → Semantic ID 生成式召回（TIGER 2023）
    → 扩散生成（DiffGRM 2025）→ GPR/OneRanker 端到端统一（2025-2026）
    → HSTU 万亿参数序列推荐（Meta 2026）

搜索：BM25/向量检索 → RAG（检索+生成）→ 生成式搜索（直接生成文档ID，2024+）
    → Agentic RAG（多步推理+工具调用，2025+）

广告：eCPM 竞价排序 → 生成式 CTR 预估 → 生成式广告创意（RLHF对齐，2025+）
    → GPR for Ads 统一召回+精排+出价（2026）
```

---

## 🔗 跨文献共性规律（2026-03-27补充）

| 规律 | 推荐 | 搜索 | 广告 |
|------|------|------|------|
| Semantic ID/Token | 物品→RQ-VAE→token | 文档→BERT→token | 广告→CLIP→token |
| 生成方式 | 自回归(GPR/OneRanker)/扩散(DiffGRM) | RAG/生成式检索 | 生成式CTR/创意/GPR统一 |
| Stage Gap解决 | GPR-rec/OneRanker/Align3GR | Agentic RAG多步 | GPR-ads统一3阶段 |
| 延迟挑战 | 高（Speculative Dec/线性注意力） | 高（需缓存/蒸馏） | 极高（实时竞价不可用） |
| 当前阶段 | 工业规模（Meta HSTU万亿参数） | RAG 已普及 | 初步生产（微信GPR） |
| Scaling Law | ✅ HSTU验证（10B→1T持续提升） | 未专项验证 | 未专项验证 |

---

## 🎓 面试考点（5条）

### Q1: 判别式 vs 生成式推荐的核心区别？
**30秒答案**：判别式——给每个 user-item pair 打分，从候选集中选最高分；生成式——直接"生成"推荐结果（item 的 Semantic ID 序列），不需要预定义候选集。
**追问方向**：生成式推荐能解决什么判别式不能的问题？答：长尾物品覆盖（不需要在 ANN 索引中被检索到）、跨模态推荐（统一文本/图像/视频的生成框架）。

### Q2: Semantic ID 的生成方式？
**30秒答案**：①RQ-VAE（Residual Quantization）：将物品 embedding 量化为多级 codebook token [C1, C2, C3]，层级越深越精细；②BSQ（Binary Scalar Quantization）：二值化编码。
**追问方向**：Semantic ID 和传统 Item ID 的区别？答：传统 ID 是随机分配的数字，没有语义；Semantic ID 编码了物品的内容语义，相似物品的 ID 相近。

### Q3: 自回归 vs 扩散模型在推荐中的对比？
**30秒答案**：AR（TIGER）：逐 token 生成，与 LLM 兼容，可控性强，但有 exposure bias 和单向依赖限制。Diffusion（DiffGRM）：并行生成所有 token，无单向依赖，但可控性差、推理需要多步去噪。
**追问方向**：哪个更有前景？答：短期看 AR（与 LLM 生态兼容），长期看可能融合（先扩散生成粗方案，再 AR 精细化）。

### Q4: RAG 和生成式搜索的关系？
**30秒答案**：RAG = 检索（传统方法找相关文档）+ 生成（LLM 合成答案），是"检索辅助生成"。生成式搜索 = 模型直接生成答案和来源引用，不需要传统检索环节。RAG 是过渡方案，生成式搜索是终态。
**追问方向**：为什么现在还用 RAG 而非完全生成式？答：LLM 幻觉问题未解决，检索提供的事实锚定仍不可或缺。

### Q5: 生成式广告的可行性？
**30秒答案**：①生成式 CTR 预估：用 LLM 理解广告语义直接预测 CTR，冷启动效果好但延迟高；②生成式广告创意：LLM 根据产品信息自动生成文案/图片，降低广告主门槛；③当前阶段：创意生成已商用，CTR 预估仍在实验。

---

### Q6: 搜广推三个领域的技术共性？
**30秒答案**：①都需要召回+排序架构；②都用 CTR/CVR 预估模型；③都面临冷启动问题；④都需要实时特征系统；⑤都可以用 LLM 增强。差异主要在约束条件和评估指标。

### Q7: 多目标优化在三个领域的应用？
**30秒答案**：广告：收入+用户体验+广告主 ROI；推荐：CTR+时长+多样性+留存；搜索：相关性+新鲜度+权威性+多样性。方法共通：Pareto/MMoE/PLE/Scalarization。

### Q8: 偏差问题在三个领域的表现？
**30秒答案**：广告：位置偏差+样本选择偏差；推荐：流行度偏差+曝光偏差；搜索：位置偏差+呈现偏差。解决方法类似：IPW/因果推断/去偏训练。

### Q9: 端到端学习的趋势和挑战？
**30秒答案**：趋势：统一模型替代分层管道（OneRec/GPR/OneRanker）。挑战：①推理效率（一个大模型 vs 多个小模型）；②可控性差（难以插入业务规则）；③调试困难（黑盒）。

### Q10: 面试中如何体现跨领域理解？
**30秒答案**：①用类比说明（如广告出价≈搜索 LTR）；②指出技术迁移（如 DIN 从推荐到广告）；③提出统一视角（如多目标在三领域的共通框架）；④结合实际经验说明如何借鉴。

### Q11（新增）: GPR同时出现在推荐和广告领域，它们的核心思想有何共同点和差异？
**30秒答案**：共同点：①都用"生成式预训练+多任务微调"范式；②都解决Stage Gap（召回-精排-出价目标不一致）；③都将推荐/广告视为序列生成问题。差异：广告GPR需要统一竞价逻辑（出价头），推荐GPR更注重GMV/留存对齐；广告场景延迟约束更严格，通常需要知识蒸馏到轻量模型。

### Q12（新增）: HSTU验证了推荐系统的Scaling Law，这意味着什么？
**30秒答案**：与LLM一样，推荐模型参数量从10B扩展到1T，NDCG等离线指标持续提升，且在线A/B也正向。意义：推荐系统可以像LLM那样"炼大模型"，大厂（Meta级别）的万亿参数推荐模型将成为护城河。工程挑战：需要3D并行（模型并行+数据并行+流水线并行）。

### Q13（新增）: OneRanker生成分支和排序分支如何协同？
**30秒答案**：共享Transformer主干，生成分支负责自回归生成候选ID（无需ANN检索），排序分支对给定候选集输出精确打分（listwise排序）。无候选集时做生成式召回，有候选集时做精排，同一个模型两种用法，消除召回-精排的Stage Gap。互相学习：排序任务让生成分支学习"用户会选哪个"，生成任务让排序分支学习"全局候选分布"。

## 🌐 知识体系连接

- **上游依赖**：自回归模型、扩散模型、Semantic ID/RQ-VAE、大规模分布式训练
- **下游应用**：下一代推荐引擎（HSTU级别）、对话式搜索、智能广告全链路投放
- **相关 synthesis**：`rec-sys/synthesis/生成式推荐系统技术全景_2026.md`、`ads/synthesis/广告系统智能化升级路径_2026.md`、`cross-domain/synthesis/端到端统一架构消除StageGap.md`（2026-03-27新建）
